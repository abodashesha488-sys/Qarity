class AdminServiceHelper {
  static void openField(String fieldName) {
    // Open the specified admin field in the admin dashboard
    // This would typically navigate to the appropriate section
    // For now, just navigate to the admin dashboard
  }
}